#! /bin/bash

for FILE1 in `ls | grep html | grep -v html~ ` ; do
echo $FILE1
cat $FILE1 | grep onsubmit | while read line; do
echo "$line" 
done
cat $FILE1 | grep onblur | while read line; do
echo "$line" 
done
echo "---------"
done
